# Auto Mode Environment

Generated by `/auto-mode-setup` (posture=enterprise, scope=project, depth=shell) on 2026-08-17. Informs auto-mode's judgment calls in this project — trust boundaries, what counts as sensitive, and what's out of scope here.

## Org-wide

- **Organization**: None configured
- **Cloud provider(s)**: None configured
- **Repository visibility**: assume private unless the remote host and repo name indicate otherwise, or a visibility check in the transcript shows public — Carter-portfolio (github.com/cargroff-cpu/Carter-portfolio) visibility was not queryable here; based on repo name/content this is a personal portfolio site, treat as private unless confirmed otherwise
- **Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary
- **Secrets management**: None configured
- **Default / protected branches**: default branch is main; protected-branch/ruleset status not queryable here
- **CI/CD deploy targets**: None configured
- **Network posture**: None configured
- **Source control**: The trusted repo (github.com/cargroff-cpu/Carter-portfolio) and its origin remote only — no additional orgs configured
- **Trusted internal domains**: None configured
- **Trusted cloud buckets**: None configured
- **Key internal services**: None configured
- **Internal package registry**: None configured
- **Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar — note .gitignore excludes .env, .env.local, .env*.local and one redacted-name pattern in this repo
- **Data retention / declassification**: None configured
- **Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)
- **Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic
- **Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment

## User-specific

- **Primary use of Claude Code**: software development
- **Trusted repo**: the Carter Groff portfolio site repo at /Users/cartergroff/Documents/Carter Groff Website code, with origin remote https://github.com/cargroff-cpu/Carter-portfolio.git — a static single-page site (React/Babel via CDN, no build step) plus an admin CMS (Admin.html/admin.jsx) served at /scaffold
- **Org-specific CLIs**: None configured

## Notes from the proposal run

- Org repo split, sibling-repo docs, cross-repo evidence, and recent usage across all projects were NOT gathered — user picked just-this-project scope.
- Repo visibility, rulesets, and protected-branch status were not queryable via `gh` — best-effort from repo facts only, not `gh`-confirmed.
- Config scans found nothing for cloud buckets, key internal services, or internal package registry — left at shipped defaults.
- Existing permissions.allow in `.claude/settings.local.json` had no classifier-bypassing or destructive entries, so no removals were proposed.
